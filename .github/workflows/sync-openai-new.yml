name: 🚀 自动同步 calciumion/new-api 到 GHCR

on:
  workflow_dispatch:  # 手动触发
  schedule:
    - cron: '0 2 * * *'  # 每天凌晨 2 点（北京时间上午 10 点）自动同步

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      packages: write  # 必需：允许推送镜像到 GHCR
      contents: write  # 必需：允许更新README文件
      # 添加删除包的权限
      packages: delete

    steps:
      - name: 🛠️ 准备环境
        run: |
          echo "✅ 开始同步流程..."
          echo "⏰ 当前时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          # 判断触发方式
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "TRIGGER_TYPE=手动触发" >> $GITHUB_ENV
          else
            echo "TRIGGER_TYPE=自动执行" >> $GITHUB_ENV
          fi

      - name: 📥 检出仓库
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔐 登录到 GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        id: login
        continue-on-error: false

      - name: 🗑️ 删除GHCR上的所有镜像版本
        run: |
          echo "🗑️ 正在删除GHCR上的所有镜像版本..."
          
          # 安装gh CLI
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null
          sudo apt update
          sudo apt install gh -y
          
          # 登录gh CLI
          echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token
          
          # 获取所有包版本并删除
          PACKAGE_ID=$(gh api graphql -f query='query{repository(owner:"${{ github.repository_owner }}", name:"openai-new"){package(name:"openai-new"){id}}}' --jq '.data.repository.package.id')
          echo "Package ID: $PACKAGE_ID"
          
          # 获取所有版本
          VERSIONS=$(gh api graphql -f query="query{node(id:\"$PACKAGE_ID\"){... on Package{versions(first:100){nodes{id,version}}}}}" --jq '.data.node.versions.nodes[].id')
          
          for VERSION_ID in $VERSIONS; do
            echo "删除版本ID: $VERSION_ID"
            gh api --method DELETE "/packages/${{ github.repository_owner }}/openai-new/versions/$VERSION_ID" || true
          done
          
          echo "✅ 所有旧版本已删除"

      - name: 🗑️ 清理本地缓存
        run: |
          echo "🗑️ 清理本地Docker缓存以确保获取最新镜像..."
          docker system prune -f
          docker rmi ghcr.io/bqizkm01918t/openai-new:latest 2>/dev/null || true
          docker rmi calciumion/new-api:latest 2>/dev/null || true
          echo "✅ 清理完成"

      - name: 📦 强制拉取 Docker Hub 上的最新镜像
        run: |
          echo "📦 正在强制拉取最新镜像: calciumion/new-api:latest"
          # 使用正确的docker pull命令（不支持--no-cache参数）
          docker pull calciumion/new-api:latest
          echo "✅ 拉取成功！"
          
          # 获取源镜像的摘要信息用于验证
          SOURCE_DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' calciumion/new-api:latest)
          echo "源镜像摘要: $SOURCE_DIGEST"
          echo "SOURCE_DIGEST=$SOURCE_DIGEST" >> $GITHUB_ENV

      - name: 🏷️ 重命名镜像为 openai-new
        run: |
          echo "🔄 重命名镜像: calciumion/new-api -> ghcr.io/bqizkm01918t/openai-new"
          docker tag calciumion/new-api:latest ghcr.io/bqizkm01918t/openai-new:latest
          echo "✅ 重命名完成！"

      - name: 🚀 推送镜像到 GHCR
        run: |
          echo "📤 正在推送: ghcr.io/bqizkm01918t/openai-new:latest"
          docker push ghcr.io/bqizkm01918t/openai-new:latest
          echo "✅ 推送完成！镜像已覆盖更新。"

      - name: 🧪 验证镜像是否推送成功
        run: |
          echo "🔍 验证镜像是否成功推送到GHCR..."
          
          # 清除本地缓存，然后从GHCR拉取镜像进行验证
          docker rmi ghcr.io/bqizkm01918t/openai-new:latest 2>/dev/null || true
          docker pull ghcr.io/bqizkm01918t/openai-new:latest
          
          # 获取推送后镜像的摘要信息
          PUSHED_DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' ghcr.io/bqizkm01918t/openai-new:latest)
          echo "推送后镜像摘要: $PUSHED_DIGEST"
          
          # 验证镜像是否存在
          docker images | grep -E 'bqizkm01918t/openai-new'
          if [ $? -eq 0 ]; then
            echo "✅ 验证通过：镜像已成功推送到GHCR并可以拉取。"
          else
            echo "❌ 验证失败：镜像未找到！"
            exit 1
          fi

      - name: 📝 获取镜像信息
        id: image_info
        run: |
          # 获取镜像大小
          IMAGE_SIZE=$(docker images --format "table {{.Repository}}:{{.Tag}}\t{{.Size}}" | grep "ghcr.io/bqizkm01918t/openai-new:latest" | awk '{print $2}')
          echo "IMAGE_SIZE=$IMAGE_SIZE" >> $GITHUB_ENV
          
          # 获取镜像创建时间
          IMAGE_CREATED=$(docker images --format "table {{.Repository}}:{{.Tag}}\t{{.CreatedAt}}" | grep "ghcr.io/bqizkm01918t/openai-new:latest" | awk '{print $2" "$3" "$4" "$5}')
          echo "IMAGE_CREATED=$IMAGE_CREATED" >> $GITHUB_ENV
          
          # 获取当前日期
          CURRENT_DATE=$(date '+%Y-%m-%d %H:%M:%S %Z')
          echo "CURRENT_DATE=$CURRENT_DATE" >> $GITHUB_ENV

      - name: 📄 更新 README
        run: |
          # 创建新的README内容
          cat > README.md << EOF
          # OpenAI-New API 镜像同步仓库
          
          ## 📋 项目简介
          
          本仓库自动同步 [calciumion/new-api](https://hub.docker.com/r/calciumion/new-api) Docker 镜像到 GitHub Container Registry (GHCR)。
          
          ## 🚀 镜像信息
          
          - **镜像地址**: \`ghcr.io/bqizkm01918t/openai-new:latest\`
          - **镜像大小**: ${{ env.IMAGE_SIZE }}
          - **源镜像**: \`calciumion/new-api:latest\`
          - **同步频率**: 每日自动同步 (北京时间上午 10 点)
          - **源镜像摘要**: ${{ env.SOURCE_DIGEST }}
          
          ## 📅 同步记录
          
          | 同步时间 | 触发方式 | 状态 |
          |----------|----------|------|
          | ${{ env.CURRENT_DATE }} | ${{ env.TRIGGER_TYPE }} | ✅ 成功 |
          
          ## 🔄 同步流程
          
          1. 删除GHCR上的所有旧版本镜像
          2. 清理本地Docker缓存
          3. 从 Docker Hub 强制拉取最新 \`calciumion/new-api\` 镜像
          4. 重新标记为 \`ghcr.io/bqizkm01918t/openai-new\`
          5. 推送到 GitHub Container Registry
          6. 验证镜像是否成功推送
          7. 更新本 README 文件
          
          ## 📦 使用方法
          
          ### 使用 SQLite 数据库
          
          \`\`\`bash
          # 使用SQLite
          docker run --name new-api -d --restart always -p 3000:3000 -e TZ=Asia/Shanghai -v /home/ubuntu/data/new-api:/data ghcr.io/bqizkm01918t/openai-new:latest
          \`\`\`
          
          ### Docker Compose
          
          \`\`\`yaml
          version: '3'
          services:
            openai-new:
              image: ghcr.io/bqizkm01918t/openai-new:latest
              container_name: new-api
              restart: always
              ports:
                - "3000:3000"
              environment:
                - TZ=Asia/Shanghai
              volumes:
                - /home/ubuntu/data/new-api:/data
          \`\`\`
          
          ## ⚙️ 配置说明
          
          - **端口映射**: 容器内 3000 端口映射到主机 3000 端口
          - **时区设置**: 设置为亚洲/上海时区
          - **数据持久化**: 主机目录 \`/home/ubuntu/data/new-api\` 挂载到容器内 \`/data\` 目录
          - **自动重启**: 容器异常退出时自动重启
          
          ## ⚠️ 注意事项
          
          - 本镜像仅作同步用途，请勿用于生产环境
          - 如需了解原始项目详情，请访问 [calciumion/new-api](https://hub.docker.com/r/calciumion/new-api)
          - 镜像每日自动同步，可能存在延迟
          - 请确保主机目录 \`/home/ubuntu/data/new-api\` 存在且有适当权限
          
          ## 📊 统计信息
          
          - **最后同步时间**: ${{ env.IMAGE_CREATED }}
          - **同步触发方式**: ${{ env.TRIGGER_TYPE }}
          - **同步状态**: 成功
          
          ---
          
          *此 README 由 GitHub Actions 自动更新*
          EOF
          
          echo "✅ README 已更新"

      - name: 🔀 提交并推送更改
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md
          git commit -m "📝 自动更新 README - ${{ env.TRIGGER_TYPE }} - ${{ env.CURRENT_DATE }}"
          git push

      - name: 📝 输出最终镜像地址
        run: |
          echo "🎉 同步完成！"
          echo "📍 镜像地址: ghcr.io/bqizkm01918t/openai-new:latest"
          echo "📅 完成时间: $(date '+%Y-%m-%d %H:%M:%S %Z')"
          echo "🔄 触发方式: ${{ env.TRIGGER_TYPE }}"
